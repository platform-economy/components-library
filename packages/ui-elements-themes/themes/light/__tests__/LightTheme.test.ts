import { LightTheme } from '../';

describe('ui-elements-themes', () => {
  describe('LightTheme', () => {

    it('should export light theme correctly', () => {
      expect(LightTheme).toBeDefined();
    });

  });
});
