import { TestTheme } from '..';

describe('ui-elements-themes', () => {
  describe('TestTheme', () => {

    it('should export test theme correctly', () => {
      expect(TestTheme).toBeDefined();
    });

  });
});
