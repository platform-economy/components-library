import { Fonts } from '../fonts';

describe('ui-elements-themes', () => {
  describe('Fonts', () => {
    it('should correctly export Fonts', () => {
      expect(Fonts).toBeDefined();
    });
  });
});
